# Evaluations

This directory stores evaluation schemas, rules, and future evaluation runners.

Keep raw test data in `tests/datasets/`. Keep reusable mock inputs in
`tests/fixtures/`. Evaluation logic belongs here.
