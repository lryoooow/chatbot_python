# Provider Response Fixtures

Store mock AI provider responses here.

Use this directory for OpenAI-compatible non-streaming responses, streaming SSE
chunks, provider error payloads, and provider-specific edge cases.
